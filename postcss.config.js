// Exporta a configuração padrão para o PostCSS
export default {
  // Define os plugins que o PostCSS vai utilizar
  plugins: {
    // Plugin do TailwindCSS — responsável por gerar as classes utilitárias
    tailwindcss: {},

    // Plugin autoprefixer — adiciona prefixos automáticos para compatibilidade entre navegadores
    autoprefixer: {},
  },
};